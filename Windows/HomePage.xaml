<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SimpleWinUI.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Select a Process:" Margin="0,0,0,10"/>
            <ComboBox x:Name="ProcessComboBox" Width="300" Margin="0,20,0,20"/>
            <TextBlock Text="Memory Address:" Margin="0,0,0,10"/>
            <TextBox x:Name="AddressTextBox" Width="300" Margin="0,0,0,20"/>
            <Button Content="Read Memory" Click="OnReadMemoryClick" Margin="0,0,0,20"/>
            <TextBlock x:Name="CurrentValueTextBlock" Text="Current Value:" Margin="0,0,0,10"/>
            <TextBox x:Name="NewValueTextBox" Width="300" Margin="0,0,0,20"/>
            <Button Content="Write Memory" Click="OnWriteMemoryClick" />
        </StackPanel>
    </Grid>
</Page>
